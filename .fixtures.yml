fixtures:
  symlinks:
    dockeragent: "#{source_dir}"
  repositories:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "4.12.0"
    docker:
      repo: "puppetlabs/docker"
      ref: "3.7.0"
